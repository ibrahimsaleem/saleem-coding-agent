/**
 * `@ibrahimsaleem/dsh-client-ui-free-model-router` — node half. The browser
 * plugin (the Settings ▸ Free Model Router panel) lives in `src/client/index.ts`.
 * @module @ibrahimsaleem/dsh-client-ui-free-model-router
 */

/** No host-side contribution; the panel is browser-only. */
export function apply(): void {}
